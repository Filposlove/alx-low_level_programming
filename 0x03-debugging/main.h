#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>

void positive_or_negative(int i);
int largest_number();
void print_remaining_day(int day,int weak,int year);
int convert_day(int day,int month)
#endif
